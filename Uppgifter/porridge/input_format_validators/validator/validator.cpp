#include "validator.h"

void run() {
    int maxN = 500000;
    int maxM = 1'000'000'000;
    int maxI = 1'000'000'000;

    int N = Int(1, maxN);
    Space();
    int M = Int(1, maxM);
    Endl();

    for (int j = 0; j < N-1; j++) {
        int imp = Int(1, maxI);
        Space();
    }
    int imp = Int(1, maxI);

    Endl();

    Eof();

}
