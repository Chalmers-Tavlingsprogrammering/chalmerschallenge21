#include "validator.h"

void run() {
    int maxN = 100000;
    int maxM = 1'000'000'000;
    int maxI = 1'000'000'000;

    int N = Int(0, maxN);
    Space();
    int M = Int(0, maxM);
    Endl();

    for (int j = 0; j < N; j++) {
        int imp = Int(1, maxI);
        Space();
    }
    Endl();

    Eof();

}
